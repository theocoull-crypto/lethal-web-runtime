# LETHAL WEB runtime

Small browser-runtime mirror used by the LETHAL WEB Google Apps Script launcher.

The JavaScript, CSS, and three.js runtime are kept in this separate repository so jsDelivr can serve them with the
same direct-CDN loading pattern used by other Google Apps Script games. Large game assets are not stored here.
